You are an expert full‑stack developer and UI/UX designer. Your task is to perform a comprehensive redesign and feature enhancement on the `xiaowu_app` project, ensuring all work aligns with the design system and visual quality of the reference `@campus-connect-marketplace-main/` codebase.

## Phase 1: Full Project Analysis & Evaluation

1. **Run a complete design & code analysis** on the entire `@xiaowu_app` project.
2. **Identify and list all pages** that currently have poor, unprofessional, or inconsistent design compared to `campus-connect-marketplace-main`.
3. **Verify the existence of the following profile‑related pages/sections:**
   - My Listings
   - Favorites
   - Wallet
   - Language Settings
   - Currency Settings
   - Notifications Settings
4. **Check the current Settings page** and note every option/toggle it contains.
5. **Consult the `@api_report` file** to understand all available backend endpoints, especially those related to:
   - User profile (fetch/update profile picture)
   - Notifications (fetch, mark as read, preferences)
   - Listings (my listings, favorites)
   - Wallet/Balance
   - User preferences (language, currency)

## Phase 2: Feature Implementation – Image Selection & Search

- **Add functionality** that allows a user to either:
  - **Choose a picture from the device photo library** (use the appropriate native/cross‑platform file picker or image picker package).
  - **Search for an image** (clarify: if this refers to searching within the app’s marketplace, ensure the search logic is integrated; if it's a web image search, implement accordingly).
- **Integrate this feature** where appropriate (e.g., updating profile picture, adding images to a new listing).
- **Ensure the selected image is correctly uploaded and displayed** (connect to the respective API endpoint for image upload/profile update).

## Phase 3: Profile Page & Sub‑Pages Redesign & Unification

All the following sections must be updated or created from scratch, with **exactly the same professional style and component library** as used in `@campus-connect-marketplace-main/`.

### A. Profile Header / Main Section
- **Fetch and display the user's profile picture** from the API in the correct placement (avatar circle at the top/left of the profile screen).
- Ensure fallback initials/placeholder if no image exists.

### B. Sub‑Pages to Redesign / Create

1. **My Listings Page**
   - Display user's posted items.
   - Connect to the correct API endpoint (GET `/api/listings/user` or similar from `api_report`).
   - Use the same card design as the main marketplace feed.

2. **Favorites Page**
   - Display saved/bookmarked items.
   - Connect to GET `/api/favorites` endpoint.
   - Ensure heart icons and toggles work and update state.

3. **Wallet Page**
   - Show current balance, transaction history.
   - Connect to wallet/balance API endpoints.
   - **Create this page if missing**.

4. **Language Settings Page**
   - List available languages.
   - Update user preference via API.
   - **Create this page if missing**.

5. **Currency Settings Page**
   - List available currencies.
   - Update user preference and reflect price changes app‑wide.
   - **Create this page if missing**.

6. **Notifications Page**
   - **CRITICAL:** Connect to the notification API.
   - Fetch list of notifications (GET endpoint).
   - Implement "Mark as Read" interaction (PATCH/PUT endpoint).
   - Display notification items with proper timestamps and read/unread styling.

### C. Profile Page Restructuring
- **Move all options currently found in the *Settings Page*** into the main Profile Page (or a dedicated "Settings" section within Profile).
- **Remove the standalone Settings page** if it is now redundant, or redirect it to the new Profile screen.
- Ensure the following options are clearly accessible from the Profile screen: My Listings, Favorites, Wallet, Language, Currency, Notifications, Logout, Delete Account (if any).

## Phase 4: API Integration & Data Flow

- **Strictly follow the specifications in `@api_report`.**
- **Connect all the above pages** to their corresponding endpoints.
- **Implement loading states and error handling** (skeleton loaders, toast messages for failures) consistent with `campus-connect-marketplace-main`.
- **Test the notification connection:** Verify that toggling notification preferences in settings and receiving new notifications works correctly.

## Phase 5: Final Polish & Consistency Check

1. **Run a second design evaluation** to ensure **every page** now meets the professional standard of `campus-connect-marketplace-main`.
2. **Fix any remaining "bad design" pages** identified in Phase 1.
3. **Ensure responsiveness** on different device sizes.
4. **Check for correct theming** (colors, fonts, border radius) matches the reference codebase.

## Output Expectations

- Provide a **summary of changes** made to each file.
- Include **code snippets** for critical new components (e.g., the new Profile layout, the Image Picker logic).
- If any API endpoints are missing or unclear in the `api_report`, list them as **Questions** before writing code.

Start by analyzing the `@xiaowu_app` structure and the `@api_report` content. Then proceed with the implementation plan.